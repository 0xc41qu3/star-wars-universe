/**
 * Available comparison operators for filtering tabular data.
 */
export const COMPARISON_OPERATOR = {
  EQUAL: "0",
  GREATER_THAN: "1",
  LESS_THAN: "-1",
};

/**
 * Available options for sorting tabular data.
 */
export const SORT = {
  ASC: "asc",
  DESC: "desc",
};
